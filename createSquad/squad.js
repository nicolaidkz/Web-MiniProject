let $navButtons = $("#navGrid").children("a");



onload = function()
{
    // lets do things to the navButtons!
    $navButtons.addClass("navButton");  // give them style
                                        // maybe give them a href to parts of the page?
}